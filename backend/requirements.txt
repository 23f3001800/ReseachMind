# Core API
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
python-multipart

# LangChain / LangGraph
langchain
langchain-core
langchain-community
langchain-groq
langchain-tavily
langchain-text-splitters
langgraph

# Search
# `ddgs` is the current package; the old `duckduckgo-search` name is no longer
# what langchain-community imports, and its absence makes every free-tier
# search fail silently and fall back to model memory.
ddgs

# HTTP
requests
httpx

# RAG (optional — needs ~1GB RAM, see requirements-rag.txt)
# pip install -r requirements-rag.txt

# Frontend (run separately — not needed for API server)
# see frontend/requirements.txt

# Testing (dev only)
# pip install pytest ruff