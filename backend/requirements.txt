# Core API
fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
python-multipart

# LangChain / LangGraph
langchain
langchain-core
langchain-community
langchain-groq
langchain-tavily
langchain-text-splitters
langgraph

# Search
duckduckgo-search

# HTTP
requests
httpx

# RAG (optional — install for document upload + vector search)
# pip install faiss-cpu langchain-huggingface sentence-transformers pypdf

# Frontend (run separately — not needed for API server)
# pip install streamlit sseclient-py

# Testing (dev only)
# pip install pytest ruff